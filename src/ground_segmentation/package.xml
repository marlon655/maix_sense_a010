<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>ground_segmentation</name>
  <version>1.0.0</version>
  <description>A C++ library for ground_segmentation of pointcloud</description>
  <maintainer email="haider_khan.lodhi@dfki.de"></maintainer>
  <license>BSD 3-Clause</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>pcl_conversions</depend>
  <depend>pcl_ros</depend>
  <depend>perception_pcl</depend>
  <depend>nanoflann</depend>

  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>

  <export>
    <!-- The upstream project installs a standard CMake package config and
         does not call ament_package(). Treat it as a generic CMake package so
         colcon generates the correct environment hooks. -->
    <build_type>cmake</build_type>
  </export>
</package>
